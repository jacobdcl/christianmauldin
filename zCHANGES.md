HOME: instead of a traditional carousel in the hero section, i want to have a modern carousel where it just shows the images one by one with a fade in and out effect, they should show in their original dimensions with no border or background. they should also automatically iterate through the images. these images will be taken from a sanityio content type called "HomeCarousel". the the only thing you should see in a full sized window is the carousel with a button underneath that says shop prints >, nothing to scroll down to. make the shop prints buttonjust like how the button looks on the current home page. if you click the button it should go to the prints page. as always this page should be extremely responsive and look good on all devices. also the carousel should be responsive and look good on all devices like all photos regardless of size or dimension should 'fit' into the carousel containerand should iterate randomly dont include any order.

PRINTS: the prints page should pull from a "Prints" sanityio content type. it should be a grid of those interactive cards like i have on the current prints page, pulling every print from the content type. the schema for prints shold be... each one has an image upload, a title, a description, and 4 sizes which are the price, the width, the height, and the size name. the sizes should have default values for sizes and prices but should also be able to be filled out by the client when they create the print item in the sanity studio. when you press on the cards for each one it should open a modal with the print details and a button to purchase like i have now. the modal should have a close button in the top right corner like all other modals on the site. i never finished those interactive cards for prints so please finish it as in make the modal include 4 sizes as buttons that you can selectand a button to purchase. that becomes available when you select a size. dont worry about the button doing anything yet well get to that later. keep the styling that i have for the size buttons and the purchase button, the airbnb modern style its awesome.

im getting rid of photos and videos page.

PERSONAL: this page will pull from another content type called "Personal" in sanityio. each item in this type will have a schema with... a title, a description, and an images upload the max the client should be able to upload is 30. each will appear as their own card in a grid like prints, except when you press these, they should open a modal wiht consistent syling/features and it will be a carousel of all the images uploaded to each "Personal" item. the idea is he will upload items in "Personal" like "Portraits", "Landscapes", "Events", "Family", etc. and when you click on the card it will open a modal with the carousel of images from each. dont worry about those examples becuase he will be uploading his own. ill make some test examples for him to see once u make the schema and content type or whatever.


WORK: this page will pull from another content type called "Work" in sanityio. each item in this type will have a schema with... a title, a description, and an images upload the max the client should be able to upload is 30. each will appear as their own card in a grid with everything in the modal being just like the PERSONAL page, the idea is he will upload items in "Work" like "Nike Photoshoot", "KINK BMX 2020", etc. and when you click on the card it will open a modal with the carousel of images from each.

ABOUT: this page will pull from another content type called "About" in sanityio. there will only be one item so idk if theres something different we need to do. i basically just want to make it so that he can upload a title and the description like i dont want to have to edit his 'about me' bio he should be able to do it himself in the studio.